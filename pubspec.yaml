name: student
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.11.5

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8

  # Database - Drift SQLite for Windows / Web / Android / iOS
   # Database - Drift SQLite for Windows / Web / Android / iOS
  drift: ^2.33.0
  drift_flutter: ^0.3.0
  sqlite3_flutter_libs: 0.6.0+eol
  path_provider: ^2.1.5
  path: ^1.9.0

  # Old sqflite packages - keep temporarily during migration
  sqflite: ^2.3.3
  sqflite_common_ffi: ^2.3.0
  sqflite_common_ffi_web: ^0.4.0

  # State Management
  provider: ^6.1.2

  # UI
  google_fonts: ^6.2.1
  fl_chart: ^0.68.0
  flutter_animate: ^4.5.0

  # Icons
  iconsax: ^0.0.8

  # Responsive
  flutter_screenutil: ^5.9.3
  firebase_core: ^4.8.0
  firebase_auth: ^6.5.0
  cloud_firestore: ^6.4.0

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^6.0.0

  # Drift code generator
  drift_dev: ^2.33.0
  build_runner: ^2.4.13

flutter:
  uses-material-design: true